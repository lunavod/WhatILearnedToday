// @flow

import React, {Fragment} from 'react'

<% if (useBaobab) { -%>
import { useBranch } from 'baobab-react/hooks'
<% } -%>

import styles from './styles.css'

<%_ if (usePropTypes) { -%>type PropTypes = {

}
<%_ } -%>

export default function <%= name %>(<%_ if (usePropTypes) { -%>{}: PropTypes<%_ } -%>) {
  <%_ if (useBaobab) { -%>
  const {  } = useBranch({

  })

  <%_ } -%>
  return <Fragment>Hello, <%= name %>!</Fragment>
}